<%= form_with(model: phisycal_status) do |form| %>
  <% if phisycal_status.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(phisycal_status.errors.count, "error") %> prohibited this phisycal_status from being saved:</h2>

      <ul>
        <% phisycal_status.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
