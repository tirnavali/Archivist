<%= form_with(model: toponym) do |form| %>
  <% if toponym.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(toponym.errors.count, "error") %> prohibited this toponym from being saved:</h2>

      <ul>
        <% toponym.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
