<%= simple_form_for @toponym, html: { class: "ui form"} do |f| %>
  <%= f.input :name ,input_html: { autofocus: true }%>
  <%= f.button :submit, class: "ui primary button" %>
  <%= link_to "Cancel", toponyms_path %>
<% end %>